#include <stdio.h>

int main() {
    int x = 10, y = 20;
    int sum = x + y;
    if (sum > 25) {
        printf("Sum is large: %d\n", sum);
    } else {
        printf("Sum is small: %d\n", sum);
    }
    return 0;
}
